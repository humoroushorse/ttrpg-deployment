{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "ui-dnd.fullname" . }}
  namespace: {{ include "ui-dnd.namespace" . }}
  labels:
    {{- include "ui-dnd.labels" . | nindent 4 }}
spec:
  {{- if eq (.Values.replicaCount | int) 1 }}
  minAvailable: 1
  {{- else }}
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable | default "50%" }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "ui-dnd.selectorLabels" . | nindent 6 }}
{{- end }}
