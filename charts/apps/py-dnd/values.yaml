# Default values for py-dnd
# This is a YAML-formatted file.

# Global values (inherited from parent chart)
global:
  version: "latest"

# Environment: local, dev, or prod
environment: "local"

# Application version (overrides global.version if set)
version: ""

# Number of replicas
replicaCount: 1

# Image configuration
image:
  repository: "ghcr.io/your-org/py-dnd"
  pullPolicy: "IfNotPresent"
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# Service account configuration
serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}

# Pod security context
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  fsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

# Container security context
securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

# Service configuration
service:
  type: ClusterIP
  port: 8001
  targetPort: 8001

# Database configuration
database:
  name: "dnd"

# Application binary name (for Python, this is typically the entrypoint script)
appBinary: "python"
appArgs: ["-m", "uvicorn", "py_dnd.main:app", "--host", "0.0.0.0", "--port", "8001"]

# Cookie configuration
# IMPORTANT: In production, override 'path' per deployment:
#   path="/ttrpg/dnd" to scope cookies to the DnD app path.
# The "/" default is for local development only.
cookie:
  path: "/"
  domain: ""
  secure: true
  sameSite: "lax"

# Health check configuration
# Health check configuration - temporarily disabled
livenessProbe: null
readinessProbe: null

# Resource limits and requests
resources:
  requests:
    cpu: "100m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "1Gi"

nodeSelector: {}
tolerations: []
affinity: {}
env: []
volumes: []
volumeMounts: []

# Pod Disruption Budget
podDisruptionBudget:
  enabled: false
  minAvailable: "50%"

# Horizontal Pod Autoscaler
autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

# Database Migration Configuration
migration:
  # Enable database migrations before deployment
  enabled: true
  
  # Migration job image (defaults to main application image)
  image:
    repository: "ghcr.io/your-org/py-dnd"
    tag: ""  # Defaults to application image tag
    pullPolicy: "IfNotPresent"
  
  # Database connection settings
  database:
    host: ""  # Defaults to postgresql.ttrpg-{env}.svc.cluster.local
    port: "5432"
    name: ""  # Defaults to .Values.database.name
    user: "postgres"
    password: ""  # Defaults to TEMPORARY_PASSWORD_WILL_BE_ROTATED in template
  
  # Job configuration
  backoffLimit: 3  # Retry up to 3 times on failure
  ttlSecondsAfterFinished: 86400  # Delete completed jobs after 24 hours
  
  # Additional environment variables for migration job
  env: []
  
  # Resource limits for migration job
  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  
  # Additional volumes for migration job
  volumes: []
  volumeMounts: []
